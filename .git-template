# Все работы с ветками происходят с ветки develop и заливаются в неё

# git checkout <ветка> - переход на указанную ветку с сохранением файлов
# git checkout -b <ветка> - создание новой ветки с сохранением файлов
# git pull - загрузка замерженных веток
# git branch --delete <ветка> - может пригодиться если на удаленном репозитории ветка удалена, а локально нет
# git fetch - загрузка изменений касаемо веток

# Как задача была сделана, то пушим её в репозиторий

# git push -u origin <ветка> - пушит ветку в репу ссылаясь на главную

# После заходим в репозиторий и создаем pull request
# В нем выбираем в ветку develop нашу новую
# После ждем ревью

# Так же по некоторым вопросам таким как git stash писать отдельно, если потребуется залью